<div>
    <h5>Условная отрисовка</h5>
</div>
<div id="ifElse">
    <label v-if="awesome">Vue восхитителен!</label>
    <label v-else>О, нет 😢</label>

    <h6>Условные группы с использованием v-if и template</h6>
    <template v-if="ok">
        <h1>Заголовок</h1>
        <p>Абзац 1</p>
        <p>Абзац 2</p>
    </template>
    <div v-else>
        А теперь — не видно Абзац
    </div>

    <div>
        <h6>Вариан с v-show</h6>
        <div v-show="ok">
            видно
        </div>
    </div>

</div>

@section Scripts{
<script>
    const vm1 = Vue.createApp({
        data() {
            return {
                awesome: true,
                ok: true
            }
        }
    }).mount('#ifElse');
</script>
}